services:
  boinc:
    image: ghcr.io/youruser/boinc-wcg-client:latest
    container_name: boinc
    restart: unless-stopped
    volumes:
      - boinc_data:/var/lib/boinc
    environment:
      - WCG_ACCOUNT_KEY=${WCG_ACCOUNT_KEY}
    #cpus: 2.0
    #mem_limit: 4g

volumes:
  boinc_data:
